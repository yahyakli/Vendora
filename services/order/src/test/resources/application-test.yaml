spring:
  datasource:
    url: jdbc:h2:mem:order_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  rabbitmq:
    host: localhost
    port: 5672
    username: test
    password: test
  data:
    redis:
      host: localhost
      port: 6379
      password:

vendora:
  jwt:
    secret: test-secret-key-that-is-at-least-32-characters-long
  stripe:
    secret-key: sk_test_placeholder
    webhook-secret: whsec_placeholder
